#include <cstdio>
#include <cstring>

const int MAX = 101, MOD = 1000000007;
int dp[MAX][MAX][MAX], ans[MAX][MAX];

int main(){
    int T; scanf("%d", &T);
    while(T-- > 0){
        memset(dp, 0, sizeof(dp));
        
        for(int i = 1; i < MAX; ++i){
        
        }
    }
    return 0;
}
